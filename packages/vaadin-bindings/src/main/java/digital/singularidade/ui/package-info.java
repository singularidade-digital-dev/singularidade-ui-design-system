/**
 * Singularidade Design System — Vaadin Flow theme bindings.
 *
 * <p>Bridge between {@code @singularidade/tokens} semantic CSS vars and Vaadin
 * v25 component tokens ({@code --vaadin-*}). Apps consume via {@code @Theme("singularidade")}.
 *
 * @see digital.singularidade.ui.SingularidadeTheme
 */
package digital.singularidade.ui;
