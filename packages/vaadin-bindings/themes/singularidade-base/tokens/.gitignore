# Generated at Maven build time — pulled from @singularidade/tokens (Style Dictionary)
# Source of truth: ../../../../tokens/build/css/{singularidade,integras}.{light,dark}.css
# Only defaults.css is hand-crafted and committed.
*.css
!defaults.css
!.gitignore
